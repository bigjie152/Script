name = "script"
compatibility_date = "2025-02-01"
compatibility_flags = ["nodejs_compat"]

pages_build_output_dir = ".vercel/output/static"

#
# D1 bindings are managed in Cloudflare Pages settings
# to avoid duplicate bindings during Functions publish.
